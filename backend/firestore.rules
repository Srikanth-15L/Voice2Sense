rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId}/segments/{segmentId} {
      allow read, write: if request.auth != null;
    }
  }
}
